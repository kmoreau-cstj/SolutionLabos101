#pragma once			// C'est pour s'assurer que les fonctions ne seront ajoutées qu'une seule fois dans le projet


// Dans le fichier .h on doit lister toutes les fonctions que l'on veut créer. On doit définir le prototype de la fonction : 
// 3 informations nécessaires pour créer une fonction

// typeDeREtour nomDeLaFonction (liste des paramètres);
void afficherMenu();
char validerChoix(char min, char max, char quitter);


